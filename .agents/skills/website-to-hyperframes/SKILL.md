---
name: website-to-hyperframes
description: 'Capture a website and create a HyperFrames video from it. Use when: (1) a user provides a URL and wants a video, (2) someone says "capture this site", "turn this into a video", "make a promo from my site", (3) the user wants a social ad, product tour, or any video based on an existing website, (4) the user shares a link and asks for any kind of video content. Even if the user just pastes a URL — this is the skill to use.'
---

# website-to-hyperframes — Codex entry point

Read and follow the complete [original skill instructions](../../../.claude/skills/website-to-hyperframes/SKILL.md) before doing this task. This entry point shares the existing Claude skill; the source file and its resources remain authoritative for the workflow. Do not treat this short adapter as the workflow itself.

Resolve the source skill directory from `../../../.claude/skills/website-to-hyperframes` relative to this file. Resolve its relative scripts, templates, references, assets, and `${CLAUDE_SKILL_DIR}` against that original directory, not this adapter directory. Read linked references as required by the source. Do not assume Claude environment variables are automatically set in Codex.

Translate Claude file/search/shell/image tools and `/skill-name` invocations to the available Codex equivalents. Named agents, model settings, hooks, plugin services, preview APIs, and MCP servers are dependencies, not tools installed by this adapter. Use a verified equivalent when available; report a missing required dependency rather than simulating completion.

Preserve workflow gates, human approvals, output locations, and verification requirements. Follow the current user's authorization, applicable repository guidance, and higher-priority runtime instructions. In Plan mode, perform only read-only work. Do not launch an external write, publish, or send a message solely to test this skill's installation.
