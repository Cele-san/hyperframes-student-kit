---
name: gsap
description: GSAP animation reference for HyperFrames. Covers gsap.to(), from(), fromTo(), easing, stagger, defaults, timelines (gsap.timeline(), position parameter, labels, nesting, playback), and performance (transforms, will-change, quickTo). Use when writing GSAP animations in HyperFrames compositions.
---

# gsap — Codex entry point

Read and follow the complete [original skill instructions](../../../.claude/skills/gsap/SKILL.md) before doing this task. This entry point shares the existing Claude skill; the source file and its resources remain authoritative for the workflow. Do not treat this short adapter as the workflow itself.

Resolve the source skill directory from `../../../.claude/skills/gsap` relative to this file. Resolve its relative scripts, templates, references, assets, and `${CLAUDE_SKILL_DIR}` against that original directory, not this adapter directory. Read linked references as required by the source. Do not assume Claude environment variables are automatically set in Codex.

Translate Claude file/search/shell/image tools and `/skill-name` invocations to the available Codex equivalents. Named agents, model settings, hooks, plugin services, preview APIs, and MCP servers are dependencies, not tools installed by this adapter. Use a verified equivalent when available; report a missing required dependency rather than simulating completion.

Preserve workflow gates, human approvals, output locations, and verification requirements. Follow the current user's authorization, applicable repository guidance, and higher-priority runtime instructions. In Plan mode, perform only read-only work. Do not launch an external write, publish, or send a message solely to test this skill's installation.
