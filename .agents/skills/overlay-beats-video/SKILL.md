---
name: overlay-beats-video
description: Build and iterate a multi-beat HyperFrames overlay package that drops on V2 over a separately-rendered A-roll for a long-form YouTube video. Use when Andy says "build the overlay beats", "deliver hyperframes for [video]", "I need motion graphics for [N] beats", "start the overlay package", or when the deliverable is N independent 1920x1080 compositions timed against a locked A-roll cut sheet. Encodes the production-company playbook (11 beats, 4:34 cut, cobalt+blossom register) — full visual identity, 5-phase composition pattern, anti-generic moves, verification workflow, and delivery checklist.
---

# overlay-beats-video — Codex entry point

Read and follow the complete [original skill instructions](../../../.claude/skills/overlay-beats-video/SKILL.md) before doing this task. This entry point shares the existing Claude skill; the source file and its resources remain authoritative for the workflow. Do not treat this short adapter as the workflow itself.

Resolve the source skill directory from `../../../.claude/skills/overlay-beats-video` relative to this file. Resolve its relative scripts, templates, references, assets, and `${CLAUDE_SKILL_DIR}` against that original directory, not this adapter directory. Read linked references as required by the source. Do not assume Claude environment variables are automatically set in Codex.

Translate Claude file/search/shell/image tools and `/skill-name` invocations to the available Codex equivalents. Named agents, model settings, hooks, plugin services, preview APIs, and MCP servers are dependencies, not tools installed by this adapter. Use a verified equivalent when available; report a missing required dependency rather than simulating completion.

Preserve workflow gates, human approvals, output locations, and verification requirements. Follow the current user's authorization, applicable repository guidance, and higher-priority runtime instructions. In Plan mode, perform only read-only work. Do not launch an external write, publish, or send a message solely to test this skill's installation.
